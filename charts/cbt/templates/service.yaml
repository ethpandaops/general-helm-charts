apiVersion: v1
kind: Service
metadata:
  name: {{ include "cbt.fullname" . }}
  labels:
    {{- include "cbt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ include "cbt.httpPort" . }}
      targetPort: http
      protocol: TCP
      name: http
  {{- if .Values.extraPorts }}
    {{ toYaml .Values.extraPorts | nindent 4}}
  {{- end }}
  selector:
    {{- include "cbt.selectorLabels" . | nindent 4 }}
