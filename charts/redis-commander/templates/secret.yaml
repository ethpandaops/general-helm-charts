apiVersion: v1
kind: Secret
metadata:
  name: {{ include "redis-commander.fullname" . }}-env
  labels:
    {{- include "redis-commander.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.secretEnv }}
  {{ $key }}: {{ $value | b64enc }}
{{- end }}
