apiVersion: v1
kind: Service
metadata:
  name: {{ include "dispatchoor-api.fullname" . }}
  labels:
    {{- include "dispatchoor-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dispatchoor-api.selectorLabels" . | nindent 4 }}
