apiVersion: v1
kind: Service
metadata:
  name: {{ include "dispatchoor-ui.fullname" . }}
  labels:
    {{- include "dispatchoor-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dispatchoor-ui.selectorLabels" . | nindent 4 }}
